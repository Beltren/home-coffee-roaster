import 'package:flutter/material.dart';

class StartRoastingPage extends StatelessWidget {
  const StartRoastingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Placeholder content
    return Scaffold(
      appBar: AppBar(
        title: const Text('Start Roasting'),
      ),
      body: const Center(
        child: Text('Start Roasting Page'),
      ),
    );
  }
}
