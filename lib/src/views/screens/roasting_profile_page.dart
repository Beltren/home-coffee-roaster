import 'package:flutter/material.dart';

class RoastingProfilePage extends StatelessWidget {
  const RoastingProfilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Placeholder content
    return Scaffold(
      appBar: AppBar(
        title: const Text('Roasting Profile'),
      ),
      body: const Center(
        child: Text('Roasting Profile Page'),
      ),
    );
  }
}
